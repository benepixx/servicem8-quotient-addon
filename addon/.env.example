# Addon configuration is handled via ServiceM8 Developer Console settings.
# See README.md for full setup instructions.
#
# The following settings are configured in the SM8 Developer Console:
#   - SM8 Queue UUID — Parts to Order
#   - SM8 Queue UUID — Ready to Book
#
# Secrets for the Cloudflare Worker are configured separately in worker/.
